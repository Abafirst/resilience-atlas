{
  "name": "resilience-atlas-client",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@auth0/auth0-react": "^2.15.1",
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0",
    "@stripe/react-stripe-js": "^2.1.0",
    "@stripe/stripe-js": "^4.10.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.13.2",
    "react-youtube": "^10.1.0",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^6.0.2",
    "vite": "^5.4.21"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:mobile": "vite build --mode production",
    "preview": "vite preview",
    "cap:build": "npm run build:mobile && npx cap sync android",
    "cap:sync": "npx cap sync android",
    "android": "npm run build:mobile && npx cap sync android && npx cap open android",
    "android:open": "npx cap open android",
    "android:run": "npx cap run android"
  }
}

// Existing content of the pdfService.js file

function buildJourneyMapPage(doc, data) {
    // Outline logo centred at the top of the page.
    // Removed logo rendering block

    // other existing code...
}
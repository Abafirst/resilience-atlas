const express = require('express');
const app = express();

// Updated routes
const quizzesRoutes = require('./routes/quiz');
const paymentsRoutes = require('./routes/stripe');

// ... rest of the file

# Railway Configuration for Resilience Atlas

[build]
  command = "npm install"

[deploy]
  startCommand = "node backend/index.js"
  healthcheckPath = "/health"
  healthcheckTimeout = 30
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3